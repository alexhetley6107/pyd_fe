import { Button } from '@/shared/ui';

const ResetPassword = () => {
  return (
    <div>
      <Button onClick={() => console.log('ResetPassword')}>
        ResetPassword
      </Button>
    </div>
  );
};

export default ResetPassword;
