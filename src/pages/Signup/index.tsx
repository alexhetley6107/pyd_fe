import { Button } from "@/shared/ui";

const Signup = () => {
  return (
    <div>
      <Button full onClick={() => console.log("Signup")}>
        Signup
      </Button>
    </div>
  );
};

export default Signup;
